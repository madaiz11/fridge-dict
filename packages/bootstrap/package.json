{
  "dependencies": {
    "dotenv": "^17.3.1"
  },
  "devDependencies": {
    "typescript": "^5.9.2"
  },
  "files": ["dist"],
  "main": "dist/index.js",
  "name": "@frigdict/bootstrap",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "pnpm run build",
    "test": "pnpm run build && node --test test/load-env.test.mjs"
  },
  "types": "dist/index.d.ts",
  "version": "1.0.0"
}
