{
  "dependencies": {
    "@frigdict/date": "workspace:*",
    "@frigdict/types": "workspace:*",
    "@nestjs/common": "10",
    "@nestjs/core": "10",
    "nestjs-pino": "^4.6.0",
    "pino": "^10.3.1",
    "pino-http": "^11.0.0",
    "reflect-metadata": "^0.2.2",
    "rxjs": "^7.8.1",
    "uuid": "^13.0.0"
  },
  "devDependencies": {
    "@types/express": "^5.0.5",
    "jest": "^30.2.0",
    "typescript": "^5.9.2"
  },
  "files": ["dist"],
  "main": "dist/index.js",
  "name": "@frigdict/logger",
  "private": true,
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "prepare": "pnpm run build",
    "test": "pnpm --dir ../.. -r --filter @frigdict/types --filter @frigdict/date --filter @frigdict/logger run build && jest --runInBand test/logger.module.test.cjs"
  },
  "types": "dist/index.d.ts",
  "version": "1.0.0"
}
